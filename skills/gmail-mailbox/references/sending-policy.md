# Sending Policy

V1:

- No sending.
- No Gmail drafts.
- No marking read, archiving, or deleting unless the requesting user asks.

Future V2:

- Create Gmail drafts after requesting user approval.
- Still require explicit approval before send.
- Sending may require additional OAuth scopes.

Do not add send scopes until the workflow is tested read-only.
